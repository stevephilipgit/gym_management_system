# Database
DATABASE_URL=

# Auth — REQUIRED, server will not start without these
JWT_ACCESS_SECRET=
FIELD_ENCRYPTION_KEY=

# Email — optional, email features disabled if not set
SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASS=

# Google Sheets — optional, sync disabled if not set
GOOGLE_CLIENT_EMAIL=
GOOGLE_PRIVATE_KEY=
GOOGLE_SHEET_ID=

# App
NODE_ENV=
APP_URL=
